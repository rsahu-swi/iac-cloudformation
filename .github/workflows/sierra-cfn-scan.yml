name: sierra-cfn-scan 

on: [push]

jobs:
  ## all scans
  sierra-cfn-scan:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: rsahu-swi/sierra-cfn-scan@v4
      with:
        cloudformation_directory: "."
        scanner: "all"
